#!/bin/bash

alias v="nvim"
alias ff="fzf --preview 'bat --style=numbers --color=always --line-range :500 {}'"
alias ls="eza --icons=always"
alias ll="eza --icons=always --long --all --header"
alias tree="eza --icons=always --total-size --tree"
alias lzg="lazygit -ucd ~/.config/lazygit"
alias lzd="lazydocker"
alias ssh='TERM=xterm-256color ssh'
alias cat="bat"
alias y="yazi"

# cd -> zoxide, with a fallback. zoxide is loaded by zinit in turbo mode (wait"1"), so `z`
# does not exist yet for roughly the first second of a shell — and in a non-interactive
# shell that sources this file (an agent's Bash tool, `zsh -c`), turbo never fires at all,
# so `z` never exists. A plain `alias cd="z"` fails with "command not found: z" in both
# windows. A function that falls back to `builtin cd` keeps the shortcut and the builtin.
cd() {
	if (( $+functions[z] )); then
		z "$@"
	else
		builtin cd "$@"
	fi
}

# custom git aliases
alias gfp="git fetch --prune"

# Neovim Aliases
alias nvim-lazytest="NVIM_APPNAME=lazytest nvim"
alias nvim-nvchad="NVIM_APPNAME=nvchad nvim"
alias nvim-kickstart="NVIM_APPNAME=kickstart nvim"
alias nvim-cybernvim="NVIM_APPNAME=cybernvim nvim"
alias nvim-astronvim="NVIM_APPNAME=astronvim nvim"
