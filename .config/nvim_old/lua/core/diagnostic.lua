vim.diagnostic.config({
  virtual_text = {
    prefix = "",
  },
  severity_sort = true,
  underline = true,
  update_in_insert = false,
  float = {
    source = "always",
    severity_sort = true,
  },
})
