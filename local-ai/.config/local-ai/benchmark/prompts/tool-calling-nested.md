Investigate the `payments-api` OOM incident using only the supplied read-only tools. First obtain pod events, then
obtain the Deployment manifest, then submit a candidate patch to the dry-run validator. Never skip validation and
never claim that the patch was applied.
