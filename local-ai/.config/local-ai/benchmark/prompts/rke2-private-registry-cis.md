Design a minimal RKE2 server configuration for a benchmark environment that enables the CIS profile and uses a private
registry mirror for `docker.io`. State which configuration belongs in `config.yaml` and which belongs in
`registries.yaml`. Do not invent credentials. Explicitly mark any version-dependent claims that require documentation
verification.
